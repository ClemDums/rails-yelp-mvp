<h1> All the restaurants </h1>

<ul>
  <% @restaurants.each do |restaurant| %>
    <li><%= restaurant.name %></li>
    <%= link_to 'See details', restaurant_path(restaurant) %>
  <% end %>
</ul>

<h2> <%= link_to 'Add a restaurant', new_restaurant_path %> </h2>
